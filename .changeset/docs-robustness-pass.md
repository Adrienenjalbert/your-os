---
"@your-os/cli": minor
---

Robustness & docs pass:

- `@your-os/cli` gains the `your-os doctor` command — a single-call health check that validates `tenant.config.ts`, runs a brand-lint sample over `src/content`, `content`, `data`, `src/data`, and (for Strapi-mode tenants) verifies the schema-as-code file is reachable. Programmatic API exported as `doctor()` + `formatReport()`.

Out-of-band but shipped together (no changeset needed — non-package changes):

- The build plan now lives in-repo at `your-os/.agents/PLAN.md`.
- README package tables auto-generated from `packages/*/package.json` `description` + `yourOs.{layer,stability}` (BEGIN/END markers, regenerated by `pnpm run check:readme --write`).
- Architecture diagram auto-generated from the real workspace dependency graph (`pnpm run check:architecture --write`).
- New umbrella scripts: `pnpm verify`, `pnpm check:docs`, `pnpm check:byte-equivalence`.
- The brand-lint byte-equivalence test against the Career Hub snapshot is now a dedicated CI job named `brand-lint byte-equivalence (REQUIRED)`, designed to be configured as a required GitHub status check.
- The configurator's Phase 4 Career Hub round-trip test now imports `careerHubConfig` from `@your-os/example-career-hub-snapshot` instead of duplicating the snapshot inline. One source of truth.
